# 調査成果サマリー

**最終更新**: 2025-11-04

このドキュメントは、遊戯王DBの調査で得られた成果物をまとめたインデックスです。

## 🆕 最新の調査結果（2025-11-04）

### カードタイプ判定方法の確立

**DOM属性ベースの判定方法**（推奨）を確立しました。テキストベースの判定はロケール依存のため非推奨としました。

```javascript
// ✅ 推奨: img要素のsrc属性を使用
const img = row.querySelector('.box_card_attribute img');
const src = img?.src || '';

let cardType;
if (src.includes('attribute_icon_spell')) {
  cardType = '魔法';
} else if (src.includes('attribute_icon_trap')) {
  cardType = '罠';
} else if (src.includes('attribute_icon_')) {
  cardType = 'モンスター';
}
```

**利点**:
- ロケール非依存（日本語、英語、韓国語すべてで動作）
- 保守性が高い（テキスト変更に影響されない）
- 信頼性が高い（画像パスは変更されにくい）

### ctypeパラメータの正確な確認

以前の調査で矛盾があった**ctypeパラメータ**の正確な値を確認しました：

- **重要**: ctypeは**selectボックス**（ラジオボタンではない）
- `""` (空文字列): すべてのカード
- `1`: モンスターカード
- `2`: 魔法カード
- `3`: 罠カード

### ブラウザJavaScriptからのFetch操作

Chrome拡張のContent Scriptsから、**fetchとDOMParserを使用してHTMLを取得・解析できる**ことを確認しました：

```javascript
// ✅ Content Script内で動作
const response = await fetch('/yugiohdb/member_deck.action?ope=1&cgid=xxx&dno=4&request_locale=ja');
const html = await response.text();
const parser = new DOMParser();
const doc = parser.parseFromString(html, 'text/html');
```

- CORS制限なし（同一オリジン）
- Chrome拡張での実装に直接使用可能

## 📄 完成した仕様書・レポート

### 1. カード検索機能 完全仕様書
**ファイル**: `card-search/parameters-complete-spec.md`

遊戯王DBカード検索APIの完全な仕様書。すべてのパラメータ、デフォルト値、動作仕様を網羅。

**主要内容**:
- 必須パラメータ（request_locale, ope, sess）
- 基本検索パラメータ（keyword, stype, ctype）
- 表示・ソートパラメータ（rp, sort）
- モンスターカード属性パラメータ（attr, species）
- モンスター数値範囲パラメータ（レベル、攻撃力、守備力など）
- リンクマーカーパラメータ（linkbtn1-9, link_m）
- モンスタータイプ・効果パラメータ（other, jogai, othercon）
- 魔法・罠効果パラメータ（effe）
- 発売日パラメータ
- URLパラメータの順序
- 複数選択パラメータの仕様
- 検証済みテストケース

### 2. カード検索機能 調査レポート
**ファイル**: `card-search/investigation-report.md`

調査プロセスと主な発見事項をまとめたレポート。

**主要内容**:
- 調査概要
- 主な発見事項
  - 隠しパラメータの発見（rp=2000, sort=10欠番）
  - 複数選択パラメータの仕様
  - リンクマーカーの仕組み
  - 範囲指定パラメータ
  - 条件結合パラメータ
  - フォーム構造の詳細（140フィールド）
- 検証済みテストケース
- URLパラメータの完全な順序
- デフォルト値一覧
- 生成データファイル一覧
- 実装への示唆
- 今後の調査項目

### 3. 汎用調査手法ガイド
**ファイル**: `investigation-methodology.md`

任意のページを調査するための汎用的な手法をまとめたガイド。

**主要内容**:
- WebSocket接続の確立方法
- 5つの調査手法（静的HTML解析、動的操作テスト、イベントリスナー調査、Network監視、JavaScript関数実行）
- 3つのデータ抽出パターン
- テストと検証の方法
- トラブルシューティング

**用途**: 今後、他のページ（デッキ一覧、カード詳細など）を調査する際のリファレンス

### 4. カード検索 再調査ガイド
**ファイル**: `card-search/reinvestigation-guide.md`

カード検索機能の仕様変更時や他言語対応時の再調査手順書。

**主要内容**:
- 5ステップの再調査手順
- 他言語対応の調査手順（en, ko, zh-CN）
- 差分確認の方法
- 実行可能なスクリプトのテンプレート

**用途**:
- 検索機能の仕様変更確認
- 他言語のi18nデータ作成
- 定期的な動作確認

### 5. API調査結果
**ファイル**: `api-investigation-results.md`

デッキ編集APIの調査結果（セッション管理、操作コード、パラメータなど）。

### 6. デッキ編集画面仕様
**ファイル**: `../design/edit/deck-edit.md`

デッキ編集画面の構造と入力フィールドの仕様。

**主要内容**:
- カード名入力フィールド（monm）の構造
- 枚数入力フィールド（monum）の構造
- カードID（monsterCardId）とイメージID（imgs）

## 📊 生成データファイル

### JSON形式のデータ

#### 検索フォーム関連
1. **tmp/search-form-fields.json**
   - 全140フィールドの生データ
   - タイプ別内訳: hidden(11), text(5), select(8), checkbox(112), radio(4)

2. **tmp/search-field-label-mappings.json**
   - パラメータ値と日本語ラベルのマッピング
   - attr, species, effe, other, jogai, otherconの全値

3. **tmp/sort-options.json**
   - ソート選択肢13種類の完全リスト
   - value, textのペア

4. **tmp/other-jogai-labels.json**
   - otherとjogaiの15個のラベル情報
   - otherconのAND/OR情報

#### テスト結果
5. **tmp/attribute-search-results-v2.json**
   - 属性検索のテスト結果（3属性）
   - 実際のURL記録

6. **tmp/multiple-params-test-results.json**
   - 複数パラメータ組み合わせテスト（9パターン）
   - 攻撃力、守備力、レベル、Pスケール、カードタイプ別

7. **tmp/linkmarker-test-results.json**
   - リンクマーカーボタンの構造情報
   - チェックボックス9個の詳細

8. **tmp/linkmarker-checked-results.json**
   - リンクマーカー選択テスト結果（3パターン）
   - AND/OR条件の動作確認

9. **tmp/multiple-checkbox-results.json**
   - 複数チェックボックステスト結果（5パターン）
   - attr, species, other, jogai, otherconの動作確認

#### 多言語対応データ
10. **tmp/wip/card-search-params-i18n.json**
    - 日本語ロケール（ja）の完全i18nデータ
    - パラメータ値とラベルの対応表
    - メタデータ（デフォルト値、複数選択可能パラメータ、範囲パラメータ）

### テストスクリプト

#### 検索パラメータテスト
1. **tmp/test-attribute-search-v2.js**
   - 属性検索のテストスクリプト
   - フォームsubmit実装

2. **tmp/test-multiple-params.js**
   - 複数パラメータ組み合わせテスト
   - 攻撃力、守備力、レベル、Pスケール、カードタイプ

3. **tmp/test-linkmarker-checked.js**
   - リンクマーカーチェック状態のテスト
   - AND/OR条件の確認

4. **tmp/test-multiple-checkbox-values.js**
   - 複数チェックボックス値のテスト
   - attr, species, other, jogaiの複数選択

#### データ抽出スクリプト
5. **tmp/extract-sort-options.js**
   - ソート選択肢の抽出
   - 検索結果ページから取得

6. **tmp/extract-other-jogai-labels.js**
   - otherとjogaiのラベル抽出
   - 検索フォームから取得

#### フォーム調査スクリプト
7. **tmp/inspect-search-form.js**
   - 検索フォームの構造調査
   - submitボタンの特定

## 🔑 重要な発見事項

### 1. 隠しパラメータ
- **rp（Results Per Page）**: UIには10/25/50/100のみ表示されるが、実際には1〜2000まで指定可能
- **sort値の欠番**: sort=10は存在しない（1-9, 11-12, 20-21の13種類）

### 2. 複数選択の実装方式
同じパラメータ名を繰り返す方式で実装：
```
attr=11&attr=12&attr=14  # 光、闇、炎属性
species=1&species=15     # ドラゴン族、戦士族
other=1&other=9          # 効果、シンクロ
```

### 3. リンクマーカーの9方向
```
linkbtn7   linkbtn8   linkbtn9
   ↖️        ⬆️        ↗️
linkbtn4    [中央]    linkbtn6
   ⬅️                 ➡️
linkbtn1   linkbtn2   linkbtn3
   ↙️        ⬇️        ↘️
```
注: linkbtn5は存在しない

### 4. 条件結合パラメータ
- `othercon`: other/jogaiの結合（1=AND, 2=OR）
- `link_m`: リンクマーカーの結合（1=AND, 2=OR）

### 5. 140フィールドの内訳
| タイプ | 数量 |
|-------|------|
| hidden | 11 |
| text | 5 |
| select | 8 |
| checkbox | 112 |
| radio | 4 |
| **合計** | **140** |

## 📈 調査の進捗

### 完了した調査項目
- ✅ デッキ編集画面の構造（カード名・枚数入力）
- ✅ カード検索フォームの完全解析（140フィールド）
- ✅ 検索パラメータの網羅的テスト
- ✅ 隠しパラメータの発見と検証
- ✅ 複数選択パラメータの動作確認
- ✅ リンクマーカー機能の完全解明
- ✅ i18nデータ構造の作成（日本語）
- ✅ 完全仕様書とレポートの作成
- ✅ **DOM属性ベースのカードタイプ判定方法** (2025-11-04)
- ✅ **ctypeパラメータの正確な値の確認** (2025-11-04)
- ✅ **ブラウザJavaScriptからのfetch操作確認** (2025-11-04)

### 次の調査項目
- ⏳ 検索結果ページのHTML構造（詳細確認）
- ⏳ カード詳細ページの構造
- ⏳ カード削除操作の調査
- ⏳ セッション管理の詳細
- ⏳ 他言語での動作確認
- ⏳ カードIDの自動解決メカニズム

## 🛠️ 実装への活用

### パラメータビルダー
```typescript
// 複数選択パラメータの実装例
const params = new URLSearchParams();
attributes.forEach(attr => params.append('attr', attr));
// 結果: attr=11&attr=12&attr=14
```

### デフォルト値の処理
```typescript
// 空文字列もパラメータとして送信
params.append('keyword', keyword || '');
params.append('ctype', ctype || '');
```

### rp=2000の活用
```typescript
// 全件取得が必要な場合
params.append('rp', '2000');
// ただし、キャッシュ戦略が重要
```

## 📝 ドキュメント階層

```
docs/
├── research/
│   ├── INVESTIGATION_SUMMARY.md          # このファイル（インデックス）
│   ├── investigation-methodology.md      # 汎用調査手法ガイド
│   ├── api-investigation-results.md      # デッキ編集API調査結果
│   ├── README.md                         # 調査ディレクトリの説明
│   ├── card-search/                      # カード検索機能
│   │   ├── README.md                     # カード検索ドキュメントの目次
│   │   ├── parameters-complete-spec.md   # 完全仕様書
│   │   ├── investigation-report.md       # 調査レポート
│   │   ├── reinvestigation-guide.md      # 再調査ガイド
│   │   └── parameters-initial.md         # 初期調査結果
│   └── archive/                          # 古い調査ファイル
│       ├── 01-initial-investigation.md
│       ├── 02-auth-flow.md
│       ├── 03-deck-api.md
│       └── 04-api-key-verification.md
└── design/
    └── edit/
        └── deck-edit.md                   # デッキ編集画面仕様

tmp/
├── search-form-fields.json                # 全140フィールド
├── search-field-label-mappings.json       # ラベルマッピング
├── sort-options.json                      # ソート選択肢
├── other-jogai-labels.json                # その他条件ラベル
├── attribute-search-results-v2.json       # 属性検索結果
├── multiple-params-test-results.json      # 複数パラメータテスト
├── linkmarker-test-results.json           # リンクマーカー構造
├── linkmarker-checked-results.json        # リンクマーカーテスト
├── multiple-checkbox-results.json         # 複数チェックボックステスト
├── wip/
│   └── card-search-params-i18n.json      # i18nデータ（日本語）
├── test-attribute-search-v2.js            # 属性検索テスト
├── test-multiple-params.js                # 複数パラメータテスト
├── test-linkmarker-checked.js             # リンクマーカーテスト
├── test-multiple-checkbox-values.js       # 複数チェックボックステスト
├── extract-sort-options.js                # ソート選択肢抽出
├── extract-other-jogai-labels.js          # ラベル抽出
└── inspect-search-form.js                 # フォーム構造調査
```

## 🎯 次のステップ

### 短期（調査フェーズ継続）
1. 検索結果ページの構造調査
2. カード詳細ページの構造調査
3. カード追加/削除操作の調査
4. セッション管理の詳細調査

### 中期（設計フェーズ）
1. 完全なAPI仕様書の作成
2. データモデル設計の確定
3. Chrome拡張機能のアーキテクチャ設計

### 長期（実装フェーズ）
1. APIラッパーの実装
2. HTMLパーサーの実装
3. UIコンポーネントの実装
4. テスト基盤の整備

---

**注意**: tmpディレクトリ内のファイルは一時的なもので、gitignoreされています。本番実装時には適切な場所に移動する必要があります。

# セッション情報テンプレート

# ブラウザのChrome DevToolsで以下を実行してセッション情報を取得:
# document.cookie

# 取得した情報を以下に設定（.env.local にコピーして使用）
# .env.local は .gitignore に追加すること

# JSESSIONID (必須)
JSESSIONID=your_jsessionid_here

# cgid - ユーザー/セッション識別子（デッキAPI使用時に必要）
CGID=your_cgid_here

# ytkn - CSRFトークン（デッキ編集時に必要）
YTKN=your_ytkn_here

# その他のcookie（必要に応じて）
AWSALB=
AWSALBCORS=

# 注意事項:
# - これらの値は個人のセッション情報なので絶対に公開しないこと
# - .env.local は必ず .gitignore に追加すること
# - セッションには有効期限があるため、定期的に更新が必要
